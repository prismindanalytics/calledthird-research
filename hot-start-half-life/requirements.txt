# Hot-Start Half-Life — Python dependencies
# Both methodology lanes (Bayesian + ML) covered by one requirements file.

# Data sourcing
pybaseball>=2.2
requests>=2.31

# Core data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scipy>=1.11

# Bayesian (Claude) lane
numpyro>=0.13
jax>=0.4
jaxlib>=0.4
ruptures>=1.1

# ML (Codex) lane
lightgbm>=4.0
scikit-learn>=1.3
quantile-forest>=1.3

# Charts (only needed if you want to regenerate them locally)
matplotlib>=3.7
seaborn>=0.13
