No ship-stopper remains on the core B1/B2 resolution, but I would tighten the language before publication.

1. **Synthesis correctness.** I fully concede the schema artifact diagnosed in `reviews/claude-review-of-codex.md` was real. My original `plate_z_norm` cross-season comparison was not publishable. After rerunning in absolute coordinates, the sign dispute is resolved: `codex-analysis/adjudication_results.json` gives `+40.46%`, and `reviews/adjudication_results_clean.json` gives `+49.40%`. My residual concern is now magnitude precision, not direction. `+40-50%` is a defensible publishable range; I would not publish `45%` as a point estimate. The two replays differ by ~8.9 attribution points, but only ~0.073 walk-rate points, which is small relative to the +0.817 pp YoY spike. The bin-level geometry is consistent with my abs-coord rerun: larger negative top band (`n_cells=1800`, mean `-7.70pp`) and smaller positive bottom band (`851`, `+5.84pp`).

2. **Claims I would soften in `reviews/ADJUDICATION_SUMMARY.md`.** `:69-70` overstates the residual as “pitcher behavior”; that remainder is really unmodeled behavior/mix/adaptation, not cleanly pitchers. `:54-63` overstates the aggregate `scripts/debug_counterfactual.py` result as a sign-locking proof. The `+0.64/+0.65pp` strike surplus is a good sanity check, but not standalone proof because it ignores count-state leverage and PA sequencing. `:69` should also soften “late-count” unless you explicitly label it as an inference. Most importantly, `:83` should not say “first-pitch only” generically: `reviews/adjudication_results_clean.json` shows `count_0_0_only = -20.41%` but `first_called_pitch_only = +11.93%`, so narrow variants are definition-sensitive.

3. **Sharpest defensible headline.** *ABS took away the high strike and explains roughly half of the early walk spike.*

4. **Missing disclosures.** State that the attribution estimate is on the apples-to-apples window `2025-03-27` to `2025-04-14` versus `2026-03-27` to `2026-04-14`; the longer `2026-04-22` frame is for H2 seasonality, not the counterfactual. Also disclose that `reviews/adjudication_results_clean.json` leaves `5.40%` of all-pitch replays on continuation-probability tails. That is not fatal, but it is real.
